# Numeronym Sites — Environment Variables
# Rename this file to .env and fill in values
# NEVER commit .env to the repo

# Cloudflare (set in Cloudflare Pages dashboard, not needed locally)
# CF_API_TOKEN=
# CF_ACCOUNT_ID=

# Future: API keys for interactive features
# OPENAI_API_KEY=          # For embedding explorer (e8s)
# ANTHROPIC_API_KEY=       # For parameter playground (p8s)
